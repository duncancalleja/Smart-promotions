name: Publish dashboards to GitHub Pages

on:
  workflow_dispatch: {}
  schedule:
    # Cron is UTC. This is Monday 07:15 UTC (~09:15 Malta during summer time).
    - cron: "15 7 * * 1"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build LIVE dashboards
        env:
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          if [ -z "${DATABRICKS_TOKEN:-}" ]; then
            echo "Missing secret DATABRICKS_TOKEN. Add it in Settings → Secrets and variables → Actions."
            exit 1
          fi
          mkdir -p site
          cp docs/campaign-cost-calculator.html site/campaign-cost-calculator.html
          cp docs/pages-hub.html site/dashboards.html
          if [ ! -f docs/smart-promo-roi.html ]; then
            echo "Missing docs/smart-promo-roi.html (same pattern as the calculator: commit a built snapshot)."
            echo "Build locally, then:"
            echo "  python3 build_smart_promo_roi_dashboard.py --country-code mt --lookback-days 90 --output docs/smart-promo-roi.html"
            exit 1
          fi
          cp docs/smart-promo-roi.html site/smart-promo-roi.html
          python build_bad_orders_dashboard.py \
            --country-code mt \
            --year 2026 \
            --output site/index.html

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

